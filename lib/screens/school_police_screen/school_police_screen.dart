import 'package:flutter/material.dart';

class SchoolPoliceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('School Police Screen'),
      ),
      body: Center(
        child: Text('Welcome to the School Police Screen!'),
      ),
    );
  }
}
